sample:
  description: PSL sample, the simplest Zephyr application
  name: PSL
common:
    tags: introduction
    integration_platforms:
      - npcx4m8f_evb
    harness: console
    harness_config:
      type: multi_line
      regex:

tests:
  sample.board.npcx_evb:
    tags: psl
    filter:
    harness: console
